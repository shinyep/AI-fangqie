# 代码审核 Todo

更新时间：2026-05-13

## P0：核心功能缺陷

- [x] 提示词预设只返回标题，导致写作页无法把预设正文传给 AI。
  - 修复：`getStylePresets()` 返回 `{ title, content }`，写作页合并风格预设和写作要求预设正文。
- [x] 切换模型只更新 `provider` 和 `model`，不更新 `api_base`。
  - 修复：模型配置补充 `api_base`，切换模型时同步写入。

## P1：数据链路问题

- [x] 星月热词在未传 `subcategory` 时直接返回空数组。
  - 修复：`subcategory` 改为可选参数，无值时请求全量热词。
- [x] 星月 `books-by-ids` 只搜索前三页，后续榜单书籍会漏查。
  - 修复：提高检索窗口为最多 10 页、每页 100 条。
- [x] 星月榜单类型在前后端重复硬编码。
  - 修复：后端 `rank-types` 增加 `source` 字段，前端按 `source === 'xingyue'` 分流。

## P2：健壮性问题

- [x] 模型列表完全依赖硬编码。
  - 修复：有 API Key 时优先请求当前供应商 `/models`，失败再回退内置列表。
- [x] 启动脚本静默吞掉后端启动错误，依赖缺失时不友好。
  - 修复：启动前检查前后端 `node_modules`，后端日志写入 `server.out.log` / `server.err.log`。

## P3：一致性与维护性

- [x] 标签控制器响应格式和其他接口不一致。
  - 修复：去掉手动 `{ code, data }` 包装，交给统一加密中间件处理。
- [x] 多处 `ensureTable()` 与启动初始化有重叠。
  - 处理：保留现状作为服务层防御式初始化，不做删除型重构，避免影响独立导入服务时的可用性。

## 验证

- [x] 后端改动文件通过 `node --check`。
- [x] 前端通过 `npm run build`。

## 追加复查：内置提示词链路

- [x] 系统种子提示词在标签初始化之后插入，导致分类标签没有自动关联。
  - 修复：新增提示词标签同步逻辑，创建、更新、种子初始化和写作预设读取时都会补齐关联。
- [x] 写作页没有读取提示词库写入的 `localStorage.activePrompt`。
  - 修复：写作页启动时读取一次选中的提示词，加入 AI 请求的提示词上下文，并清理本地缓存。
- [x] 写作服务用选中的提示词替换基础系统提示，可能让模板变量残留并破坏默认写作约束。
  - 修复：选中提示词改为追加到基础系统提示的 `选用提示词` 区块。
- [x] 创意工具箱点击内置提示词后，只把提示词塞进主题字段，后端生成器无法识别它是提示词上下文。
  - 修复：前端单独传 `prompt_content` / `prompt_title`，后端生成器拼入用户提示。
- [x] 使用临时内存数据库验证 12 条内置提示词全部可加载、全部有关联标签、全部出现在写作页预设接口。
